{#
/**
 * @file
 * Layouts: Nested 1 Column.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'layout-section',
    'layout',
    'layout--nested--1-col',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div class="row">
      {% if content.first %}
        <div class="col">
          <div {{ region_attributes.first.addClass('layout__region', 'layout__region--content', 'layout__region--first') }}>
            {{ content.first }}
          </div>
        </div>
      {% endif %}
    </div>
  </div>
{% endif %}
