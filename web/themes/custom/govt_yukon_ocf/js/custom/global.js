/**
 * @file
 * Global Theme Functions.
 */

(function ($, Drupal, once) {
  'use strict';

  // General Theme Behaviors.
  Drupal.behaviors.govtYukonOcfTheme = {
    attach: function (context, settings) {

    }
  };

})(jQuery, Drupal, once);
